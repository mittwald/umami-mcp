MCP_PORT=3334
# Optional SSRF guard. Comma-separated origin patterns; only matching
# X-Umami-Url values are accepted. `*` matches one host segment.
# Example: https://*.example.com,https://umami.acme.io
# UMAMI_URL_ALLOWLIST=
